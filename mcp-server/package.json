{
  "name": "@crabtalk/mcp-server",
  "version": "0.1.0",
  "description": "CrabTalk MCP server — P2P sync engine for Claude Code",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0",
    "diff": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/diff": "^7.0.0",
    "typescript": "^5.7.0"
  }
}
